version: '3.8'

services:
  web-local:
    build:
      context: .
      target: dev
    ports:
      - "3007:3007"
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - PORT=3007
      - NODE_ENV=development
      - TZ=Asia/Ho_Chi_Minh
